import React from 'react';

import './userlist.css';
// import { addUser, removeUser, getUser, getUsersInRoom, users } from '../../../../server/users.js'

const UserList = () => {
    // When a users array gets updated, the UserList component gets updated
    // 
    <div>

    </div>
}